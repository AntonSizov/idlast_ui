$("#comment_content").val('');
$("#comments-table").append("<%= escape_javascript(render('articles/comment_tr', :comment => @comment)) %>");
$("#new_comment input.btn").val("Create comment");
$("#new_comment input.btn").prop("disabled", null);