<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "IDLast", root_path, :class => "brand" %>
      <div class="container nav-collapse">
		<ul class="nav">
		  <%= li_link_to 'News', articles_path %>
		  <% if user_signed_in? && current_user.admin? %>
	      <li><%= link_to 'Users', users_path %></li>
		  <% end %>
		</ul> <!-- ul class nav -->
		<% if user_signed_in? %>
		<ul class="nav pull-right">
		  <li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
			  <% if current_user.name != "" %>
			  <%= current_user.name %>
			  <% else %>
			  <%= current_user.email %>
			  <% end %>
			  <b class="caret"></b>
			</a>
			<ul class="dropdown-menu">
			  <li><%= link_to "My profile", user_path(current_user) %></li>
			  <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
			</ul>
		  </li>
		</ul><!-- .nav.dropdown -->
		<% else %>

        <ul class="nav pull-right">
		  <li><div><%= link_to "Sign up", new_user_registration_path, :class => "btn btn-primary" %></div></li>
		  <li class="divider-vertical"></li>
		  <li><div><%= link_to "Sign in", new_user_session_path, :class => "btn" %></div></li>
        </ul>
		<% end %>
      </div><!--/.nav-collapse -->

    </div>
  </div>
</div>
