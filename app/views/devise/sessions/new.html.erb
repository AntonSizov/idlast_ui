<div class="row" style="margin-top:30px;">
<div class="span12">
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name),
:html => { :class => "form-signin"}) do |f| %>
  <h2 class="form-signin-heading"><%= t(:sign_in) %></h2>
  <%= devise_error_messages! %>

  <%= f.email_field :email, :autofocus => true, :placeholder => t(:your_email),
  :class => "input-block-level" %>

  <%= f.password_field :password, :placeholder => t(:your_pass),
  :class => "input-block-level" %>

  <% if devise_mapping.rememberable? -%>
    <label class="checkbox">
    <%= f.check_box :remember_me, {checked: true} %>
	<%= f.label t(:remember_me) %>
	</label>
  <% end -%>

  <div><%= f.submit t(:sign_in), :class => "btn btn-large" %></div>
  <p></p>
  <div><%= render "devise/shared/links" %></div>
<% end %>
</div>
</div>
